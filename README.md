the project build on JDBC statement


dbConfig variables:

   final private static String dbUrl = "jdbc:postgresql://localhost:5432/hotel_db";
    final private static String userName = "postgres";
    final private static String password = "postgres";
